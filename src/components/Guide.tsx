import React from 'react';
import {StyleSheet, Text} from 'react-native';

export const Guide = () => {
  return <Text style={styles.contentText}>TODO</Text>;
};

const styles = StyleSheet.create({
  contentText: {
    color: 'white',
    fontSize: 20,
  },
});
